use crate::ErrorKind;
use std::fmt;
use std::fs::File;
use std::io;
use std::io::prelude::*;
use std::mem;
use std::ops::Range;
use std::path;
use std::str;

pub fn write_file() {
    println!("exporting!!!");
}